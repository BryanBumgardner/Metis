#Numpy
import numpy as np 
arr1 = np.array([1,2,3]) #creates an array from a list. All these elements have to be the same type (int, string)
arr2 = np.array([[1,2], [3,4]]) #creates a multidimensional array 

print(arr1.shape)